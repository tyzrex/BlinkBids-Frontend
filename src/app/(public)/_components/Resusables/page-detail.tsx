export default async function PageDetails({ pageName }: { pageName: string }) {
  // const pageDetail = await getPageDetail(pageName);
  // //   //console.log(pageDetail);
  // if (pageDetail.error) {
  //   return <></>;
  // }
  return (
    <article>
      {/* <h1>{pageDetail?.title}</h1> */}
      {/* <div dangerouslySetInnerHTML={{ __html: content }}></div> */}
    </article>
  );
}
